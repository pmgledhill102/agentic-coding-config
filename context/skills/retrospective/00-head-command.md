Run a retrospective on this session: close the loop on what past retros produced, name this session's biggest costs, propose **at most three** tier-priced changes (removals with equal standing to additions), draft a per-session journal entry into `paul-context/_incoming/` (or as a `journal-draft`-labeled Issue against `pmgledhill102/paul-context` when the local clone isn't available), and route actionable findings into GitHub Issues (same-repo and cross-repo). Do **not** append to `~/.claude/retros.md` — that file is retired. Do **not** commit or push to `paul-context` from this skill — promotion lives in `/promote-journal-inbox`, run from a `paul-context` checkout (see `paul-context/decisions/2026-05-05-journal-inbox-promotion.md`).
